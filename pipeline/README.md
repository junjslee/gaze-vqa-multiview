# Data Generation Pipeline

This `pipeline/` directory is the data generation pipeline.

Primary scope:
- benchmark/sample generation
- prompt construction and task orchestration
- state handling and utility modules used for generation

Evaluation logic is separated under `pipeline/eval/`.
